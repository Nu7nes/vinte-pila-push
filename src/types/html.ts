export interface ExtractedItem {
    id?: string;
    title?: string;
    price?: string;
    link?: string;
    description?: string;
    candidates?: string;
    date?: { since?: string; publication?: string };
}
